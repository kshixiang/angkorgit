Usage:
  gitmd                            Open the current directory
  gitmd open [path]                Open the provided path
  gitmd clone [-b branch] <url>    Clone the repository by url or owner/repo
                                       (ex torvalds/linux), optionally checking out
                                       the branch

gmd is a short alias for gitmd
